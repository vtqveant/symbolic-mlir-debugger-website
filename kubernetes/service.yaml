apiVersion: v1
kind: Service
metadata:
  name: mlir-debugger-website
  namespace: default
  labels:
    app: mlir-debugger-website
spec:
  selector:
    app: mlir-debugger-website
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  type: ClusterIP